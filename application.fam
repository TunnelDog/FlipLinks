# For details & more options, see documentation/AppManifests.md in firmware repo

App(
    appid="fliplinks",  # Must be unique
    name="FlipLinks",  # Displayed in menus
    apptype=FlipperAppType.EXTERNAL,
    entry_point="fliplinks_app",
    requires=["gui"],
    stack_size=2 * 1024,
    fap_icon="fliplinks.png",
    fap_category="Misc",

)
